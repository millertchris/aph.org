{
  "name": "aph-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end Playwright tests for APH.org WooCommerce",
  "scripts": {
    "test": "playwright test",
    "test:shop": "playwright test tests/e2e/shop.spec.ts",
    "test:product": "playwright test tests/e2e/product.spec.ts",
    "test:cart": "playwright test tests/e2e/cart.spec.ts",
    "test:checkout": "playwright test tests/e2e/checkout.spec.ts",
    "test:account": "playwright test tests/e2e/account.spec.ts",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "dependencies": {
    "dotenv": "^17.4.0"
  }
}
